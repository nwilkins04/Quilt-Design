import React, {Component} from "react";

class Save extends Component {
    render() {
        return(
            <button className="saveButton">Save Design</button> 
        )
    }
};

export default Save;